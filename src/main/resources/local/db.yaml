spring:
  sql:
    init:
      mode: always
      data-locations: classpath:local/data.sql
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
